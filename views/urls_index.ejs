<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
</head>
  <body>
    <ul>
      <% for(var shortUrl in urls) { %>
        <li><a href="#"><%= shortUrl %></a> : <%= urls[shortUrl] %></li>
        <% }; %>
    </ul>

  </body>
</html>